# OAuth Token Status List - draft-21 Compliance Matrix

Audit of the status-list issuer/hosting implementation against
[draft-ietf-oauth-status-list-21](https://datatracker.ietf.org/doc/draft-ietf-oauth-status-list/)
(Token Status List, expires 23 December 2026). All findings below were checked directly
against the primary IETF text. Complements the `Status::ApplicationSpecific` enum work
tracked in #152.

Status legend:

- **Compliant** - behavior matches the spec, verified by reading the code (and, where noted, a test).
- **Fixed** - deviated from the spec; corrected in this change.
- **Deviation (documented)** - intentional gap; the spec marks the feature OPTIONAL and no
  ticket deliverable requires it.
- **Out of scope** - belongs to another tracked ticket or component.
- **Hardening note** - not a draft-21 violation (SHOULD-level or unspecified), but worth
  tracking as a follow-up.

| #   | Spec §      | Requirement                                                                                                                                | Status                 | Notes / evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | §4.1        | Byte array compressed with DEFLATE ([RFC 1951]) in the ZLIB ([RFC 1950]) container                                                         | Compliant              | `utils/lst_gen.rs` (`encode_compressed`) uses `flate2`'s `ZlibEncoder`/`ZlibDecoder` consistently, i.e. DEFLATE wrapped in a 2-byte zlib header + Adler-32 trailer.                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2   | §4.1        | Highest compression level RECOMMENDED                                                                                                      | Compliant              | `encode_compressed` constructs `ZlibEncoder::new(_, Compression::best())`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 3   | §4.1        | Bits packed LSB (bit "0") → MSB (bit "7") within each byte                                                                                 | Compliant              | `apply_updates`/`decode_status_array`: `byte_index = bit_position / 8`, `bit_offset = bit_position % 8`. Pinned by existing unit tests and the new §4.1 spec-vector tests (`test_spec_vector_1_bit`, `test_spec_vector_2_bit`).                                                                                                                                                                                                                                                                                                                                                                                                            |
| 4   | §4.2 / §4.3 | `bits` ∈ {1, 2, 4, 8}                                                                                                                      | Compliant              | `determine_bits()` only ever returns 1/2/4/8; `BitFlag` (`utils/bits_validation.rs`) rejects any other value on the update path.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 5   | §4.2        | `lst` is base64url of the compressed array, no padding                                                                                     | Compliant              | `base64url` crate, no `=` padding.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 6   | §4.2 / §9   | `aggregation_uri` (OPTIONAL)                                                                                                               | Deviation (documented) | Not implemented. OPTIONAL per spec; no ticket deliverable requires it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 7   | §5.1        | JWT `typ` header MUST be the abbreviated `statuslist+jwt`                                                                                  | Compliant              | `web/handlers/status_list/get_status_list.rs` (`issue_jwt`): `header.typ = Some(STATUS_LISTS_HEADER_JWT.into())`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 8   | §5.1        | `sub` MUST be the URI of the Status List Token, equal to the Referenced Token's `status_list.uri`                                          | Compliant              | Built at publish time in `publish_status.rs` as `https://{server_domain}/statuslists/{list_id}`; the route is registered in `startup.rs` as `.nest("/statuslists", ...).route("/{list_id}", get(get_status_list))`, so the claim resolves to the actual serving endpoint.                                                                                                                                                                                                                                                                                                                                                                  |
| 9   | §5.1        | `iat` REQUIRED; `exp` / `ttl` RECOMMENDED                                                                                                  | Compliant              | `StatusListToken` (`get_status_list.rs`) sets all three (`iat`, `exp = iat + TOKEN_EXP`, `ttl = TOKEN_TTL`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 10  | §5.2        | CWT protected header label `16` (type) MUST be the full media type `application/statuslist+cwt` (or the registered CoAP Content-Format ID) | **Fixed**              | Code emitted the abbreviated `statuslist+cwt` (correct for JWT `typ`, wrong for the COSE header). New constant `STATUS_LISTS_CWT_TYPE_VALUE` used in `issue_cwt()`; pinned by an assertion on `cwt.protected.header.rest` in `test_get_status_list_success_cwt`.                                                                                                                                                                                                                                                                                                                                                                           |
| 11  | §5.2        | CWT MUST NOT carry the CWT tag ([RFC 8392] §6); the COSE message MUST be `COSE_Sign1_Tagged` (18) or `COSE_Mac0_Tagged` (17)               | **Fixed**              | Code called the untagged `sign1.to_vec()`. Switched to `sign1.to_tagged_vec()` (CBOR tag 18), matching the spec's own worked example (`d2 84 58 20 ...`) and this repo's `docs/architecture.md:76` hex dump. The one in-repo consumer (the test) switched to `from_tagged_slice()` in lockstep; no other in-repo caller exists (grep-verified). This changes the CWT endpoint's wire bytes for any external consumer.                                                                                                                                                                                                                      |
| 12  | §5.2        | CWT claim keys: `2` (sub), `6` (iat), `4` (exp), `65534` (ttl), `65533` (status_list)                                                      | Compliant              | `constants.rs` (`SUBJECT=2, ISSUED_AT=6, EXP=4, TTL=65534, STATUS_LIST=65533`) already matched. The test previously asserted these via the same constants used to build the claims (tautological); hardened to assert against the literal spec integers instead.                                                                                                                                                                                                                                                                                                                                                                           |
| 13  | §4.3 / §5.2 | CBOR `status_list.lst` MUST be a CBOR byte string (major type 2) of the raw compressed bytes; `bits` MUST be a CBOR unsigned integer       | **Fixed**              | `issue_cwt()` emitted `CborValue::Text(status_record.status_list.lst.clone())` — the base64url string correct for the §4.2 JSON/JWT representation, but the wrong CBOR type for §4.3. Fixed by decoding the stored base64url `lst` back to raw bytes (`base64url::decode`, mirroring the existing `build_x5chain()` pattern in the same file) and emitting `CborValue::Bytes(...)`. `bits` was already `CborValue::Integer(...)`, correct as-is. The prior test asserted `CborValue::Text(...)` — the same wrong assumption as the code — so it didn't catch this; hardened to assert `CborValue::Bytes` against the raw compressed bytes. |
| 14  | §7.1        | Status Type registry: `0x03` and `0x0C`-`0x0F` permanently reserved as application-specific; all others reserved for future registration   | Out of scope (#152)    | `Status::ApplicationSpecific` is fixed at `3` - a spec-valid value - but can't express `0x0C`-`0x0F`. Enum redesign tracked in #152.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 15  | §8.1        | Media types `application/statuslist+jwt` / `application/statuslist+cwt` for content negotiation and `Content-Type`                         | Compliant              | `constants.rs` / `get_status_list.rs` return these verbatim as `Content-Type`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 16  | §8.2        | Successful response MUST use a 2xx status code                                                                                             | Compliant              | `get_status_list.rs` returns `StatusCode::OK`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 17  | §8.2        | `Content-Encoding` (e.g. gzip) via RFC 9110 negotiation, RECOMMENDED for Status List Tokens **in JWT format**                              | Hardening note         | Current code gzips unconditionally for both JWT and CWT responses, regardless of the client's `Accept-Encoding`, and without the JWT-only scoping the spec draws (CWT is already binary and its `lst` is already zlib-compressed, so gzipping it is unmotivated double-compression). SHOULD-level; not fixed in this change.                                                                                                                                                                                                                                                                                                               |
| 18  | §8.4        | Historical resolution via a `?time=` query parameter                                                                                       | Deviation (documented) | OPTIONAL; not implemented.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 19  | -           | Publish/update REST endpoints (`publish_status.rs`, `update_status.rs`)                                                                    | Out of scope           | This server's own management API; draft-21 governs the Status List Token/Referenced Token wire format and the GET status-list endpoint, not issuer-internal tooling.                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 20  | §10         | X.509 EKU for the signing certificate                                                                                                      | Out of scope (#136)    | Certificate provisioning (`utils/cert_manager/`) is a separately shipped component.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

[RFC 1951]: https://www.rfc-editor.org/rfc/rfc1951
[RFC 1950]: https://www.rfc-editor.org/rfc/rfc1950
[RFC 8392]: https://www.rfc-editor.org/rfc/rfc8392
