# Fixture for CI.yml's zizmor self-test -- deliberately vulnerable, do not "fix" it.
# The missing `persist-credentials: false` below is the assertion. It lives outside
# .github/ so the gate's own `inputs: .` does not collect it.
name: zizmor selftest fixture
on: [push]

permissions: {}

jobs:
  deliberately-vulnerable:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
