# Runbook Moved

This runbook moved to [`deploy/observability/runbooks/token-generation.md`](../../deploy/observability/runbooks/token-generation.md).

This compatibility stub keeps existing alert `runbook_url` links working until
one release has deployed the new `deploy/observability/...` URLs.
