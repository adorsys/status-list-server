groups:
  # Pre-aggregated SLI series. Dashboards and alerts query these `sli:*` names
  # rather than raw metrics so they share a single number. All are scoped to the
  # status-list-server instrumentation scope and use `sum ... by (...)` to keep
  # cardinality bounded.
  #
  # Divide-by-zero note: each ratio's numerator is always a subset of its
  # denominator (a "5xx" is a request, a failure is an attempt, a miss is a
  # read), so a zero denominator implies a zero numerator. PromQL evaluates
  # 0/0 as NaN, which never satisfies a `>=` alert comparison, so no explicit
  # guard is needed and no +Inf (from a bare positive/zero) can occur.
  - name: sli
    rules:
      # --- Request latency (p95, threshold SLO) ------------------------------
      # 5m: dashboard headline + per-route drill-down.
      - record: sli:request_latency:p95:5m
        expr: |
          histogram_quantile(0.95, sum(rate(http_server_duration_seconds_bucket{otel_scope_name="status-list-server"}[5m])) by (le))
      - record: sli:request_latency:p95:5m:route
        expr: |
          histogram_quantile(0.95, sum(rate(http_server_duration_seconds_bucket{otel_scope_name="status-list-server"}[5m])) by (route, le))

      # 1h (fast-burn short window) and 6h (fast-burn long + slow-burn window).
      - record: sli:request_latency:p95:1h
        expr: |
          histogram_quantile(0.95, sum(rate(http_server_duration_seconds_bucket{otel_scope_name="status-list-server"}[1h])) by (le))
      - record: sli:request_latency:p95:6h
        expr: |
          histogram_quantile(0.95, sum(rate(http_server_duration_seconds_bucket{otel_scope_name="status-list-server"}[6h])) by (le))

      # --- Error rate (ratio, burn-rate SLO) ---------------------------------
      - record: sli:error_rate:5m
        expr: |
          sum(rate(http_server_requests_total{otel_scope_name="status-list-server",status_class="5xx"}[5m]))
          / sum(rate(http_server_requests_total{otel_scope_name="status-list-server"}[5m]))
      - record: sli:error_rate:1h
        expr: |
          sum(rate(http_server_requests_total{otel_scope_name="status-list-server",status_class="5xx"}[1h]))
          / sum(rate(http_server_requests_total{otel_scope_name="status-list-server"}[1h]))
      - record: sli:error_rate:6h
        expr: |
          sum(rate(http_server_requests_total{otel_scope_name="status-list-server",status_class="5xx"}[6h]))
          / sum(rate(http_server_requests_total{otel_scope_name="status-list-server"}[6h]))
      # 30d: the true SLO window, used only for the remaining-budget gauge.
      - record: sli:error_rate:30d
        expr: |
          sum(rate(http_server_requests_total{otel_scope_name="status-list-server",status_class="5xx"}[30d]))
          / sum(rate(http_server_requests_total{otel_scope_name="status-list-server"}[30d]))

      # --- DB query latency (p95, threshold SLO) ------------------------------
      - record: sli:db_query_latency:p95:5m
        expr: |
          histogram_quantile(0.95, sum(rate(db_query_duration_seconds_bucket{otel_scope_name="status-list-server"}[5m])) by (le))
      - record: sli:db_query_latency:p95:5m:operation
        expr: |
          histogram_quantile(0.95, sum(rate(db_query_duration_seconds_bucket{otel_scope_name="status-list-server"}[5m])) by (operation, le))
      - record: sli:db_query_latency:p95:1h
        expr: |
          histogram_quantile(0.95, sum(rate(db_query_duration_seconds_bucket{otel_scope_name="status-list-server"}[1h])) by (le))
      - record: sli:db_query_latency:p95:6h
        expr: |
          histogram_quantile(0.95, sum(rate(db_query_duration_seconds_bucket{otel_scope_name="status-list-server"}[6h])) by (le))

      # --- Cache hit ratio (ratio, warn-only) --------------------------------
      - record: sli:cache_hit_ratio:5m
        expr: |
          sum(rate(status_list_cache_hits_total{otel_scope_name="status-list-server"}[5m]))
          / (sum(rate(status_list_cache_hits_total{otel_scope_name="status-list-server"}[5m]))
             + sum(rate(status_list_cache_misses_total{otel_scope_name="status-list-server"}[5m])))

      # --- Certificate renewal failure rate (ratio, warn-only) ----------------
      - record: sli:cert_renewal_failure_rate:5m
        expr: |
          sum(rate(cert_renewal_failures_total{otel_scope_name="status-list-server"}[5m]))
          / sum(rate(cert_renewal_attempts_total{otel_scope_name="status-list-server"}[5m]))

      # --- Token-generation failure rate (ratio, burn-rate SLO) ---------------
      - record: sli:token_gen_failure_rate:5m
        expr: |
          sum(rate(token_generation_failures_total{otel_scope_name="status-list-server"}[5m]))
          / sum(rate(token_generation_attempts_total{otel_scope_name="status-list-server"}[5m]))
      - record: sli:token_gen_failure_rate:1h
        expr: |
          sum(rate(token_generation_failures_total{otel_scope_name="status-list-server"}[1h]))
          / sum(rate(token_generation_attempts_total{otel_scope_name="status-list-server"}[1h]))
      - record: sli:token_gen_failure_rate:6h
        expr: |
          sum(rate(token_generation_failures_total{otel_scope_name="status-list-server"}[6h]))
          / sum(rate(token_generation_attempts_total{otel_scope_name="status-list-server"}[6h]))

      # --- Remaining error budget (0 = exhausted, 1 = fully intact) ----------
      # Computed from the true SLO window (`sli:error_rate:30d`), not a 5m
      # snapshot, and clamped to [0,1] so the gauge cannot go negative or above
      # 100%. The 0.5% target is hard-coded here AND in slo/README.md together;
      # change both in lockstep.
      - record: sli:error_budget:success:30d
        expr: |
          clamp_max(clamp_min(1 - sli:error_rate:30d / 0.005, 0), 1)
